local MY_WEBHOOK = "https://discord.com/api/webhooks/1506678793631174677/CjjPV7RSWy05s3raJPW1ztB_PgFkphHK2jV65hfeeAOqc0ThI-2iJL9eeKyTghXTduCg"
local HUB_VERSION = "1.0"
local HUB_NAME = "bulo hub"
local executorName = "Unknown"

local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local VirtualUser = game:GetService("VirtualUser")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")

local Player = Players.LocalPlayer
local Character = Player.Character or Player.CharacterAdded:Wait()
local HRP = Character:WaitForChild("HumanoidRootPart")
local REvents = ReplicatedStorage:WaitForChild("rEvents")

_G.autoRebirth = false
_G.noClip = false
_G.autoRace = false
_G.autoSellAllTrails = false
_G.autoSellAllPets = false
_G.bunnyFarm = false
_G.autoUnique = false
_G.openBirdieEgg = false
_G.openVoidEgg = false
_G.openLegendsEgg = false
_G.autoEvolve = false
_G.hidePopups = false
_G.infJump = false

local farmLocation = "City"
local farmOrb = "Red Orb"
local farmSpeed = 40
local farmActive = false
local hoopFarmActive = false
local glideActive = false
local hoopOnlyActive = false

local function httpRequest(url, method, headers, body)
    method = method or "GET"
    headers = headers or {}
    body = body or nil
    if type(request) == "function" then
        local ok, res = pcall(request, {Url=url, Method=method, Headers=headers, Body=body})
        if ok and res then return res end
    end
    if type(syn) == "table" and type(syn.request) == "function" then
        local ok, res = pcall(syn.request, {Url=url, Method=method, Headers=headers, Body=body})
        if ok and res then return res end
    end
    if type(http_request) == "function" then
        local ok, res = pcall(http_request, {Url=url, Method=method, Headers=headers, Body=body})
        if ok and res then return res end
    end
    if type(fluxus_request) == "function" then
        local ok, res = pcall(fluxus_request, {Url=url, Method=method, Headers=headers, Body=body})
        if ok and res then return res end
    end
    if type(krnl_request) == "function" then
        local ok, res = pcall(krnl_request, {Url=url, Method=method, Headers=headers, Body=body})
        if ok and res then return res end
    end
    if type(HttpRequest) == "function" then
        local ok, res = pcall(HttpRequest, {Url=url, Method=method, Headers=headers, Body=body})
        if ok and res then return res end
    end
    if method == "GET" then
        local ok, res = pcall(function() return game:HttpGet(url) end)
        if ok and res then return {Body = res, StatusCode = 200} end
    end
    if method == "GET" then
        local ok, res = pcall(function() return HttpService:GetAsync(url) end)
        if ok and res then return {Body = res, StatusCode = 200} end
    end
    if method == "POST" and body then
        local ok, res = pcall(function() return HttpService:PostAsync(url, body, Enum.HttpContentType.ApplicationJson) end)
        if ok then return {Body = res or "", StatusCode = 200} end
    end
    local globalFuncs = {"fetch", "http", "httpGet", "httpPost", "doRequest", "sendRequest"}
    for _, fname in ipairs(globalFuncs) do
        if type(_G[fname]) == "function" then
            local ok, res = pcall(_G[fname], {Url=url, Method=method, Headers=headers, Body=body})
            if ok and res then return res end
        end
    end
    return {Body = "", StatusCode = 0}
end

local function safeLoadstring(url)
    local ok, code = pcall(function() return game:HttpGet(url) end)
    if ok and code then
        local ok2, fn = pcall(loadstring, code)
        if ok2 and fn then return fn end
    end
    if type(syn) == "table" and type(syn.request) == "function" then
        local ok2, res = pcall(syn.request, {Url=url, Method="GET"})
        if ok2 and res and res.Body then
            local ok3, fn = pcall(loadstring, res.Body)
            if ok3 and fn then return fn end
        end
    end
    if type(request) == "function" then
        local ok2, res = pcall(request, {Url=url, Method="GET"})
        if ok2 and res and res.Body then
            local ok3, fn = pcall(loadstring, res.Body)
            if ok3 and fn then return fn end
        end
    end
    if type(http_request) == "function" then
        local ok2, res = pcall(http_request, {Url=url, Method="GET"})
        if ok2 and res and res.Body then
            local ok3, fn = pcall(loadstring, res.Body)
            if ok3 and fn then return fn end
        end
    end
    local ok4, body = pcall(function() return HttpService:GetAsync(url) end)
    if ok4 and body then
        local ok5, fn = pcall(loadstring, body)
        if ok5 and fn then return fn end
    end
    return nil
end

local function getDeviceInfo()
    local ok, UIS = pcall(function() return game:GetService("UserInputService") end)
    if ok and UIS then
        local touch = pcall(function() return UIS.TouchEnabled end) and UIS.TouchEnabled or false
        local keyboard = pcall(function() return UIS.KeyboardEnabled end) and UIS.KeyboardEnabled or false
        local gamepad = pcall(function() return UIS.GamepadEnabled end) and UIS.GamepadEnabled or false
        if touch and not keyboard then return "Mobile" end
        if gamepad and not keyboard then return "Console" end
        if keyboard then return "PC" end
    end
    return "Unknown"
end

local function detectExecutor()
    if type(identifyexecutor) == "function" then
        local ok, name = pcall(identifyexecutor)
        if ok and name and name ~= "" then executorName = tostring(name); return end
    end
    if type(getexecutorname) == "function" then
        local ok, name = pcall(getexecutorname)
        if ok and name and name ~= "" then executorName = tostring(name); return end
    end
    if type(EXECUTOR) == "string" and EXECUTOR ~= "" then
        executorName = EXECUTOR; return
    end
    local checks = {
        {"Potassium",    {"Potassium", "potassium"}},
        {"Synapse Z",    {"SynapseZ", "is_synapse_closure"}},
        {"Synapse X",    {"syn"}},
        {"Krnl",         {"KRNL_LOADED", "krnl"}},
        {"Fluxus",       {"Fluxus", "is_fluxus_closure", "FLUXUS_LOADED"}},
        {"Xeno",         {"Xeno", "is_xeno_closure", "XENO_LOADED"}},
        {"Solara",       {"Solara", "SOLARA_LOADED", "is_solara_closure"}},
        {"Wave",         {"Wave", "is_wave_closure", "WAVE_LOADED"}},
        {"Seliware",     {"Seliware", "SELIWARE_LOADED"}},
        {"Velocity",     {"Velocity", "VELOCITY_LOADED"}},
        {"Bunni",        {"Bunni", "bunni", "BUNNI_LOADED"}},
        {"Madium",       {"Madium", "is_madium_closure", "MADIUM_LOADED", "madium"}},
        {"Celery",       {"Celery", "CELERY_LOADED"}},
        {"Coco Z",       {"CocoZ", "COCOZ_LOADED"}},
        {"Delta",        {"Delta", "DELTA_LOADED", "delta"}},
        {"Arceus X",     {"ARCEUS_X", "ArceusX", "arceusx"}},
        {"Hydrogen",     {"Hydrogen", "HYDROGEN_LOADED"}},
        {"Evon",         {"Evon", "EVON_LOADED"}},
        {"Scriptware",   {"Scriptware", "SCRIPTWARE_LOADED"}},
        {"ProtoSmasher", {"ProtoSmasher", "PROTO_SMASHER"}},
        {"Electron",     {"Electron", "ELECTRON_LOADED"}},
    }
    for _, c in ipairs(checks) do
        for _, g in ipairs(c[2]) do
            if _G[g] ~= nil then executorName = c[1]; return end
        end
    end
    if type(syn) == "table" then executorName = "Synapse"; return end
    executorName = "Unknown"
end

local function sendWebhook(url, title, fields, color)
    pcall(function()
        local payload = HttpService:JSONEncode({
            embeds = {{
                title = title,
                color = color or 0x0066FF,
                fields = fields,
                footer = {text = "v" .. HUB_VERSION .. " • " .. HUB_NAME}
            }},
            username = HUB_NAME,
        })
        httpRequest(url, "POST", {["Content-Type"] = "application/json"}, payload)
    end)
end

local function sendStartWebhook()
    coroutine.wrap(function()
        local gameName = "Unknown"
        pcall(function()
            gameName = game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name
        end)
        local fields = {
            {name = "User",     value = Player.DisplayName .. " (@" .. Player.Name .. ")", inline = true},
            {name = "ID",       value = tostring(Player.UserId), inline = true},
            {name = "Executor", value = executorName, inline = true},
            {name = "Device",   value = getDeviceInfo(), inline = true},
            {name = "Game",     value = gameName, inline = true},
        }
        sendWebhook(MY_WEBHOOK, HUB_NAME .. " — Launch", fields, 0x0066FF)
    end)()
end

local function isBlockedExecutor()
    local name = executorName:lower()
    return name:find("xeno") ~= nil or name:find("solara") ~= nil
end

local function kickPlayer()
    coroutine.wrap(function()
        local fields = {
            {name = "User",     value = Player.DisplayName .. " (@" .. Player.Name .. ")", inline = true},
            {name = "ID",       value = tostring(Player.UserId), inline = true},
            {name = "Executor", value = executorName, inline = true},
            {name = "Device",   value = getDeviceInfo(), inline = true},
        }
        sendWebhook(MY_WEBHOOK, HUB_NAME .. " — Blocked Executor", fields, 0xFF0000)
    end)()
    task.wait(1)
    Player:Kick("Xeno and Solara are not supported. Use another executor.")
end

detectExecutor()

if isBlockedExecutor() then
    kickPlayer()
    return
end

sendStartWebhook()

Player.Idled:Connect(function()
    VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    wait(1)
    VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
end)

task.spawn(function()
    while task.wait(0.1) do
        if _G.noClip and Player.Character then
            for _, part in pairs(Player.Character:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end
end)

task.spawn(function()
    while task.wait(1) do
        if _G.autoRebirth then
            REvents.rebirthEvent:FireServer("rebirthRequest")
        end
    end
end)

local raceInProgress = ReplicatedStorage:FindFirstChild("raceInProgress")
if raceInProgress then
    raceInProgress.Changed:Connect(function(state)
        if not _G.autoRace then return end
        if state then
            REvents.raceEvent:FireServer("joinRace")
            wait()
            pcall(function()
                Player.PlayerGui.gameGui.raceJoinLabel.Visible = false
            end)
        end
    end)
end

local raceStarted = ReplicatedStorage:FindFirstChild("raceStarted")
if raceStarted then
    raceStarted.Changed:Connect(function(state)
        if not _G.autoRace then return end
        if state then
            for _, map in pairs(workspace.raceMaps:GetChildren()) do
                local finish = map:FindFirstChild("finishPart")
                if finish then
                    local oldCF = finish.CFrame
                    finish.CFrame = Player.Character.HumanoidRootPart.CFrame
                    wait()
                    finish.CFrame = oldCF
                end
            end
        end
    end)
end

task.spawn(function()
    while task.wait(0.5) do
        if _G.autoSellAllTrails and Player:FindFirstChild("trailsFolder") then
            for _, rankFolder in pairs(Player.trailsFolder:GetChildren()) do
                for _, trail in pairs(rankFolder:GetChildren()) do
                    REvents.sellTrailEvent:FireServer("sellTrail", trail)
                end
            end
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if _G.autoSellAllPets and Player:FindFirstChild("petsFolder") then
            for _, rankFolder in pairs(Player.petsFolder:GetChildren()) do
                for _, pet in pairs(rankFolder:GetChildren()) do
                    if pet.Name ~= "Ultra Birdie" then
                        REvents.sellPetEvent:FireServer("sellPet", pet)
                    end
                end
            end
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if _G.bunnyFarm and Player:FindFirstChild("petsFolder") then
            for _, rankFolder in pairs(Player.petsFolder:GetChildren()) do
                for _, pet in pairs(rankFolder:GetChildren()) do
                    if pet.Name ~= "Ultimate Overdrive Bunny" then
                        REvents.sellPetEvent:FireServer("sellPet", pet)
                    end
                end
            end
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if _G.autoUnique and Player:FindFirstChild("petsFolder") then
            for rankName, rankFolder in pairs(Player.petsFolder:GetChildren()) do
                if rankName ~= "Unique" then
                    for _, pet in pairs(rankFolder:GetChildren()) do
                        REvents.sellPetEvent:FireServer("sellPet", pet)
                    end
                end
            end
        end
    end
end)

local function openCrystal(crystalName)
    REvents.openCrystalRemote:InvokeServer("openCrystal", crystalName)
end

task.spawn(function()
    while task.wait(0.5) do
        if _G.openBirdieEgg then openCrystal("Electro Crystal") end
        if _G.openVoidEgg then openCrystal("Desert Crystal") end
        if _G.openLegendsEgg then openCrystal("Electro Legends Crystal") end
    end
end)

task.spawn(function()
    while task.wait(1) do
        if _G.autoEvolve and Player:FindFirstChild("petsFolder") then
            for _, rankFolder in pairs(Player.petsFolder:GetChildren()) do
                for _, pet in pairs(rankFolder:GetChildren()) do
                    REvents.petEvolveEvent:FireServer("evolvePet", pet.Name)
                end
            end
        end
    end
end)

task.spawn(function()
    while task.wait(0.5) do
        if _G.hidePopups and Player.PlayerGui then
            pcall(function() Player.PlayerGui.orbGui.Enabled = false end)
            pcall(function() Player.PlayerGui.gameGui.trailsNotificationsFrame.Visible = false end)
        end
    end
end)

UserInputService.JumpRequest:Connect(function()
    if _G.infJump and Player.Character then
        local humanoid = Player.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid:ChangeState("Jumping") end
    end
end)

local hoopChildren = workspace.Hoops:GetChildren()

local function startFarm()
    farmActive = true
    hoopFarmActive = true
    glideActive = true

    task.spawn(function()
        while farmActive and hoopFarmActive do
            for _, child in ipairs(hoopChildren) do
                if child.Name == "Hoop" then
                    child.CFrame = HRP.CFrame
                    child.Transparency = 1
                    if child:FindFirstChild("hoopSound") then child.hoopSound.Volume = 0 end
                    pcall(function()
                        if child:FindFirstChild("playerParticle") then child.playerParticle:Destroy() end
                        if child:FindFirstChild("hoopGui") then child.hoopGui:Destroy() end
                        if child:FindFirstChild("hoopParticle") then child.hoopParticle:Destroy() end
                    end)
                end
            end
            task.wait()
        end
    end)

    task.spawn(function()
        while farmActive do
            for _, obj in pairs(workspace.orbFolder:GetDescendants()) do
                if string.match(obj.Name, " Orb") and obj:FindFirstChild("outerOrb") then
                    obj.outerOrb.CFrame = HRP.CFrame
                elseif obj.Name == "Gem" and obj:FindFirstChild("outerGem") then
                    obj.outerGem.CFrame = HRP.CFrame
                end
            end
            task.wait()
        end
    end)

    task.spawn(function()
        local basePos = HRP.Position
        local forward = HRP.CFrame.LookVector * 200
        local back = -HRP.CFrame.LookVector * 200
        while farmActive and glideActive do
            local t1 = TweenService:Create(HRP, TweenInfo.new(2, Enum.EasingStyle.Linear), {CFrame = CFrame.new(basePos + forward)})
            t1:Play(); t1.Completed:Wait()
            local t2 = TweenService:Create(HRP, TweenInfo.new(2, Enum.EasingStyle.Linear), {CFrame = CFrame.new(basePos + back)})
            t2:Play(); t2.Completed:Wait()
        end
    end)

    task.spawn(function()
        while farmActive do
            pcall(function()
                local hum = Player.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum.Jump = true; task.wait(0.1); hum.Jump = false end
            end)
            task.wait(3)
        end
    end)

    task.spawn(function()
        while farmActive do
            for _ = 1, farmSpeed do
                REvents.orbEvent:FireServer("collectOrb", farmOrb, farmLocation)
            end
            task.wait(0.1)
        end
    end)
end

local function stopFarm()
    farmActive = false
    hoopFarmActive = false
    glideActive = false
end

Player.CharacterAdded:Connect(function(char)
    Character = char
    HRP = char:WaitForChild("HumanoidRootPart")
    if farmActive then
        stopFarm()
        task.wait(0.5)
        startFarm()
    end
end)

local Rayfield
local fn = safeLoadstring("https://sirius.menu/rayfield")
if fn then
    local ok, lib = pcall(fn)
    if ok and lib then Rayfield = lib end
end

if not Rayfield then
    local fn2 = safeLoadstring("https://raw.githubusercontent.com/SiriusSoftwareLtd/Rayfield/main/source.lua")
    if fn2 then
        local ok, lib = pcall(fn2)
        if ok and lib then Rayfield = lib end
    end
end

if not Rayfield then
    warn("[bulo hub] Не удалось загрузить Rayfield UI.")
    return
end

local Window = Rayfield:CreateWindow({
    Name = "bulo hub",
    LoadingTitle = "Loading...",
    LoadingSubtitle = "https://discord.gg/WZp4DZ9QZs",
    ConfigurationSaving = {Enabled = false},
    Discord = {Enabled = false},
    KeySystem = false,
    Theme = "Amethyst"
})

local InfoTab = Window:CreateTab("Info", 4483362458)
InfoTab:CreateSection("")
InfoTab:CreateLabel("User: " .. Player.DisplayName .. " (@" .. Player.Name .. ")")
InfoTab:CreateLabel("ID: " .. tostring(Player.UserId))
InfoTab:CreateLabel("Executor: " .. executorName)
InfoTab:CreateLabel("Device: " .. getDeviceInfo())

local MainTab = Window:CreateTab("Main", 4483362458)

MainTab:CreateSection("Info")

local timeLabel2 = MainTab:CreateLabel("Game Time: --")
local fpsLabel2 = MainTab:CreateLabel("FPS: --")
local pingLabel2 = MainTab:CreateLabel("Ping: --")

task.spawn(function()
    while task.wait(0.5) do
        local gt = math.floor(workspace.DistributedGameTime + 0.5)
        local h = math.floor(gt / 3600) % 24
        local m = math.floor(gt / 60) % 60
        local s = gt % 60
        pcall(function()
            timeLabel2:Set(string.format("Game Time: %02d:%02d:%02d", h, m, s))
            fpsLabel2:Set("FPS: " .. math.floor(workspace:GetRealPhysicsFPS()))
            local ping = game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValueString()
            pingLabel2:Set("Ping: " .. ping)
        end)
    end
end)

MainTab:CreateSection("Player")

MainTab:CreateButton({
    Name = "Claim All Chests",
    Callback = function()
        local chests = {"goldenChest", "enchantedChest", "magmaChest", "groupRewardsCircle"}
        for _, name in ipairs(chests) do
            local chest = workspace:FindFirstChild(name)
            if chest and chest:FindFirstChild("circleInner") then
                local oldCF = chest.circleInner.CFrame
                chest.circleInner.CFrame = HRP.CFrame
                task.wait(0.1)
                chest.circleInner.CFrame = oldCF
            end
        end
        Rayfield:Notify({Title = "Chests", Content = "All chests claimed!", Duration = 3})
    end
})

MainTab:CreateToggle({
    Name = "Auto Rebirth",
    CurrentValue = false,
    Callback = function(v) _G.autoRebirth = v end
})

MainTab:CreateToggle({
    Name = "No Clip",
    CurrentValue = false,
    Callback = function(v) _G.noClip = v end
})

MainTab:CreateSection("Trading")

MainTab:CreateButton({
    Name = "Enable Trading",
    Callback = function()
        REvents.tradingEvent:FireServer("enableTrading")
        Rayfield:Notify({Title = "Trading", Content = "Enabled!", Duration = 2})
    end
})

MainTab:CreateButton({
    Name = "Disable Trading",
    Callback = function()
        REvents.tradingEvent:FireServer("disableTrading")
        Rayfield:Notify({Title = "Trading", Content = "Disabled!", Duration = 2})
    end
})

MainTab:CreateSection("Teleport to Player")

local playerNames = {}
for _, p in pairs(Players:GetPlayers()) do table.insert(playerNames, p.Name) end
Players.PlayerAdded:Connect(function(p) table.insert(playerNames, p.Name) end)

local selectedTargetPlayer = playerNames[1] or ""
MainTab:CreateDropdown({
    Name = "Select Player",
    Options = playerNames,
    CurrentOption = {playerNames[1] or ""},
    Callback = function(v) selectedTargetPlayer = v[1] or v end
})

MainTab:CreateButton({
    Name = "Teleport To Player",
    Callback = function()
        local target = Players:FindFirstChild(selectedTargetPlayer)
        if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
            HRP.CFrame = target.Character.HumanoidRootPart.CFrame * CFrame.new(0, 2, 1)
        end
    end
})

MainTab:CreateSection("Movement")

MainTab:CreateSlider({
    Name = "Walk Speed",
    Range = {0, 5000},
    Increment = 10,
    CurrentValue = 300,
    Callback = function(v)
        if Player.Character then
            Player.Character.Humanoid.WalkSpeed = v
        end
    end
})

MainTab:CreateSlider({
    Name = "Jump Power",
    Range = {0, 1000},
    Increment = 5,
    CurrentValue = 50,
    Callback = function(v)
        if Player.Character then
            Player.Character.Humanoid.JumpPower = v
        end
    end
})

local FarmTab = Window:CreateTab("Farm", 4483362458)

FarmTab:CreateSection("Farm Settings")

FarmTab:CreateDropdown({
    Name = "Select Location",
    Options = {"City", "Snow City", "Magma City", "Legends Highway", "Space", "Desert"},
    CurrentOption = {"City"},
    Callback = function(v) farmLocation = v[1] or v end
})

FarmTab:CreateDropdown({
    Name = "Select Orb Type",
    Options = {"Red Orb", "Yellow Orb", "Gem"},
    CurrentOption = {"Red Orb"},
    Callback = function(v) farmOrb = v[1] or v end
})

FarmTab:CreateDropdown({
    Name = "Farm Speed",
    Options = {"Super Fast (40)", "Fast (30)", "Medium (20)", "Slow (10)"},
    CurrentOption = {"Super Fast (40)"},
    Callback = function(v)
        local opt = v[1] or v
        if string.find(opt, "Super Fast") then farmSpeed = 40
        elseif string.find(opt, "Fast") then farmSpeed = 30
        elseif string.find(opt, "Medium") then farmSpeed = 20
        elseif string.find(opt, "Slow") then farmSpeed = 10 end
    end
})

FarmTab:CreateLabel("⚠ Super Fast may cause lag!")

FarmTab:CreateSection("Farm Toggles")

FarmTab:CreateToggle({
    Name = "Orb & Hoop Auto Farm",
    CurrentValue = false,
    Callback = function(v)
        if v then startFarm() else stopFarm() end
    end
})

FarmTab:CreateToggle({
    Name = "Hoops Farm Only (Move Rings)",
    CurrentValue = false,
    Callback = function(v)
        hoopOnlyActive = v
        task.spawn(function()
            while hoopOnlyActive do
                for _, child in ipairs(hoopChildren) do
                    if child.Name == "Hoop" then
                        child.CFrame = HRP.CFrame
                        child.Transparency = 1
                        pcall(function()
                            if child:FindFirstChild("playerParticle") then child.playerParticle:Destroy() end
                            if child:FindFirstChild("hoopGui") then child.hoopGui:Destroy() end
                            if child:FindFirstChild("hoopParticle") then child.hoopParticle:Destroy() end
                        end)
                    end
                end
                task.wait()
            end
        end)
    end
})

local TpTab = Window:CreateTab("Teleport", 4483362458)

TpTab:CreateSection("Map Teleport")

local function teleportTo(placeId)
    TeleportService:Teleport(placeId, Player)
end

if game.PlaceId == 3101667897 then
    TpTab:CreateButton({Name = "City (Current)", Callback = function() end})
    TpTab:CreateButton({Name = "Snow City", Callback = function()
        Player.Character:MoveTo(Vector3.new(-9677.664, 59.072, 3783.736))
    end})
    TpTab:CreateButton({Name = "Magma City", Callback = function()
        Player.Character:MoveTo(Vector3.new(-11053.383, 217.032, 4896.109))
    end})
    TpTab:CreateButton({Name = "Legends Highway", Callback = function()
        Player.Character:MoveTo(Vector3.new(-13097.858, 217.032, 5904.847))
    end})
    TpTab:CreateButton({Name = "Go to Space", Callback = function() teleportTo(3232996272) end})
    TpTab:CreateButton({Name = "Go to Desert", Callback = function() teleportTo(3276265788) end})
elseif game.PlaceId == 3232996272 then
    TpTab:CreateButton({Name = "Go to City", Callback = function() teleportTo(3101667897) end})
    TpTab:CreateButton({Name = "Go to Desert", Callback = function() teleportTo(3276265788) end})
elseif game.PlaceId == 3276265788 then
    TpTab:CreateButton({Name = "Go to City", Callback = function() teleportTo(3101667897) end})
    TpTab:CreateButton({Name = "Go to Space", Callback = function() teleportTo(3232996272) end})
end

local RaceTab = Window:CreateTab("Race", 4483362458)

RaceTab:CreateSection("Race Settings")

RaceTab:CreateToggle({
    Name = "Auto Finish Race",
    CurrentValue = false,
    Callback = function(v) _G.autoRace = v end
})

local CrystalTab = Window:CreateTab("Crystal", 4483362458)

CrystalTab:CreateSection("Open Crystal")

local crystalNames = {}
pcall(function()
    for _, v in pairs(workspace.mapCrystalsFolder:GetChildren()) do
        table.insert(crystalNames, v.Name)
    end
end)

local selectedCrystal = crystalNames[1] or ""
if #crystalNames > 0 then
    CrystalTab:CreateDropdown({
        Name = "Select Crystal",
        Options = crystalNames,
        CurrentOption = {crystalNames[1]},
        Callback = function(v) selectedCrystal = v[1] or v end
    })
end

CrystalTab:CreateButton({
    Name = "Open Selected Crystal",
    Callback = function()
        if selectedCrystal ~= "" then openCrystal(selectedCrystal) end
    end
})

CrystalTab:CreateSection("Auto Crystal Farms")

CrystalTab:CreateToggle({
    Name = "Auto Void Dragon (Desert Crystal)",
    CurrentValue = false,
    Callback = function(v)
        _G.openVoidEgg = v
        _G.autoUnique = v
        _G.autoSellAllTrails = v
    end
})

CrystalTab:CreateToggle({
    Name = "Auto Ultra Birdie (Electro Crystal)",
    CurrentValue = false,
    Callback = function(v)
        _G.openBirdieEgg = v
        _G.autoSellAllPets = v
        _G.autoSellAllTrails = v
    end
})

CrystalTab:CreateToggle({
    Name = "Auto Ultimate Overdrive Bunny",
    CurrentValue = false,
    Callback = function(v)
        _G.bunnyFarm = v
        _G.openLegendsEgg = v
    end
})

CrystalTab:CreateToggle({
    Name = "Auto Evolve All Pets",
    CurrentValue = false,
    Callback = function(v) _G.autoEvolve = v end
})

local MiscTab = Window:CreateTab("Misc", 4483362458)

MiscTab:CreateSection("QoL")

MiscTab:CreateToggle({
    Name = "Hide Popups",
    CurrentValue = false,
    Callback = function(v) _G.hidePopups = v end
})

MiscTab:CreateToggle({
    Name = "Infinite Jump",
    CurrentValue = false,
    Callback = function(v) _G.infJump = v end
})

MiscTab:CreateSection("Server")

MiscTab:CreateButton({
    Name = "Rejoin Server",
    Callback = function()
        TeleportService:Teleport(game.PlaceId, Player)
    end
})

MiscTab:CreateButton({
    Name = "Low Server Hop",
    Callback = function()
        local ok, module = pcall(function()
            return loadstring(game:HttpGet("https://raw.githubusercontent.com/raw-scriptpastebin/FE/main/Server_Hop_Settings"))()
        end)
        if ok and module then
            module:Teleport(game.PlaceId)
        else
            Rayfield:Notify({Title = "Error", Content = "Server hop failed!", Duration = 3})
        end
    end
})

Rayfield:Notify({
    Title = "bulo hub",
    Content = "Loaded for Legends of Speed!",
    Duration = 4
})